import { PorterStatusPipe } from './porter-status.pipe';

describe('PorterStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new PorterStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
