import { PorterTypePipe } from './porter-type.pipe';

describe('PorterTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PorterTypePipe();
    expect(pipe).toBeTruthy();
  });
});
